<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
        Configuración de FileProvider para compartir archivos del cacheDir
        Usado para abrir PDFs descargados temporalmente con Intent ACTION_VIEW
    -->
    <cache-path
        name="pdf_cache"
        path="." />

    <!-- Permitir abrir PDFs guardados en getExternalFilesDir(Environment.DIRECTORY_DOCUMENTS) -->
    <external-files-path
        name="documents"
        path="Documents/" />
</paths>
